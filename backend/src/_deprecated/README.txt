Duplicate leave-request feature, superseded by LeaveRequestController/LeaveRequestModel
(routes/leave_requests.php -> /api/leave-requests), which is what the frontend (leave.js)
actually calls. Kept here for reference only -- renamed to .txt so PHP's route glob and
class autoloader ignore them. Safe to delete once you've confirmed nothing needs them.
